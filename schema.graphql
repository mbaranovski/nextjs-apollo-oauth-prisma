# import User, oAuthMethod from 'generated/prisma.graphql'

type Query {
  me: User,
}

type Mutation {
  authenticate(oAuthCode: String!, oAuthMethod: oAuthMethod!): AuthenticateUserPayload
}

type AuthenticateUserPayload {
  token: String!
}